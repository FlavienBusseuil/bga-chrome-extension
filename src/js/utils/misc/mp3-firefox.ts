
export const isSoundCustom = () => false;

export const removeCustomMp3 = async () => {
  // not compatible with FF
};

export const uploadCustomMp3 = async () => {
  // not compatible with FF
};

export const playMp3 = async () => {
  // not compatible with FF
};